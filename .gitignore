# Dependencies
node_modules/

# Environment variables / secrets — never commit real credentials
.env
.env.local
.env.*.local

# Real business configuration (not a template) — never commit
configuration/business.yaml

# Logs
*.log
npm-debug.log*

# OS / editor junk
.DS_Store
Thumbs.db
.vscode/
.idea/

# Build/cache output (future-proofing; safe to keep even if unused today)
dist/
build/
coverage/
